import { execute } from './execute'

execute({
  // Number of flips of heads or tails
  num: 10,
  // Show the sequence of flips
  showSequence: true,
  // Show the step by step of the algorithm
  showStepByStep: true
})
